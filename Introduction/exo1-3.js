var age, ageMax, denree, conso;

age= 32;
ageMax= 85;
denree= "sandwich";
conso= 1;

console.log("Il vous reste " + (ageMax-age) * (conso * 365) + " de " + denree + " avant d'atteindre l'age de " + ageMax);
//Odre operation
console.log((1 + 2) * 3 + 4 / 2);